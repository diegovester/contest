# Question 1: Weird Dictionary — 2 points


def create_weird_dictionary():
    letters = ['a', 'b', 'c', 'd', 'e']
    numbers = [1, 2, 3, 4, 5]
    # Write a dictionary comprehension to merge both the lists as key:value pairs. set letters as keys and numbers as values in one line.
    
    
    return weird_dictionary

weird_dictionary = create_weird_dictionary()
print("Weird Dictionary:", weird_dictionary)